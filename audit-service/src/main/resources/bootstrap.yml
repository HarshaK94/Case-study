server:
  port: 8765

spring:
    application:
      name: audit-service
    
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    instance-id: ${spring.application.name}-${server.port}