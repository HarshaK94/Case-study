server:
  port: 8764

spring:
  application:
    name: notification-publisher-service
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    instance-id: ${spring.application.name}-${server.port}